version: '3'

services:
  backend:
    build: .
    env_file:
      - .env
    ports:
      - 8081:8081
    restart: always
    environment:
      - TZ=America/Sao_Paulo
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - /etc/timezone:/etc/timezone:ro
      - ${PORTAL_PEL_API_FILE_UPLOAD_DIR:-./uploads}:${PORTAL_PEL_API_FILE_INTERNAL_UPLOAD_DIR:-/uploads}
    networks:
      - kafka-external

networks:
  kafka-external:
    name: pel-gateway_kafka-network
    external: true